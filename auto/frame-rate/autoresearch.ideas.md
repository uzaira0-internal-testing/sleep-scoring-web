# Autoresearch Ideas: Frame Rate

## Dead Ends (tried and failed)

## Key Insights

## Remaining Ideas
- Replace marker DOM manipulation with a single canvas overlay
- Use OffscreenCanvas for marker rendering in a worker
- Implement virtual scrolling for the data table
- Use CSS containment (`contain: layout style paint`) on chart container
- Use `pointer-events: none` on non-interactive overlays during drag
- Investigate uPlot's built-in band/series plugins for marker rendering (avoid custom DOM)
- Use IntersectionObserver to skip rendering off-screen markers
- Profile with Chrome DevTools Performance tab for actual flame graph data
