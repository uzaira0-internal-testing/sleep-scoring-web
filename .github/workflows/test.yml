# All testing runs locally via scripts/test-all.sh
# See scripts/test-all.sh for the full test suite (fast/full/heavy tiers)
#
# This workflow is intentionally empty — kept as documentation.
# GitHub Actions is used only for:
#   - build.yml: Tauri desktop app releases (triggered by tauri-v* tags)
#   - deploy.yml: Docker build + push + deploy (manual dispatch)
